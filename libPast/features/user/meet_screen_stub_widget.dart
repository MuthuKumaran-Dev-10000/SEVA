import 'package:flutter/material.dart';

Widget getInAppMeetScreen(String url) => const Scaffold(
      body: Center(
        child: Text('Platform not supported for in-app video calls'),
      ),
    );
